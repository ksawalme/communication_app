import React, { Component } from 'react';
const School = () => {
    const schoolImages = ['bath','brushteeth', 'spoon', 'tv', 'bread']

    return (
      <div>
          <p>School</p>
        {/* {HomeImages.map((name) => (
          <p>{name}</p>
          ))} */}
      </div>
    )
  };
  export default School;